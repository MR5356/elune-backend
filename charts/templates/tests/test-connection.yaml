apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elune-backend.fullname" . }}-test-connection"
  labels:
    {{- include "elune-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elune-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
